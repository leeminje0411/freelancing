<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>스튜디오 오븐</title>

  <!-- 메타, 부트스트랩, 스타일 불러오기 (질문 코드 그대로) -->
  <link rel="preload" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
        as="style" onload="this.rel='stylesheet'">
  <noscript>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  </noscript>
  <link rel="stylesheet" href="/style.css" />
  <style>
    /* html, body를 100% 높이로 만들고, flex 컨테이너 구조 설정 */
    html, body {
      margin: 0;
      padding: 0;
      height: 100%;           /* 화면 전체 높이를 차지하도록 */
      display: flex;          /* flex 컨테이너 */
      flex-direction: column; /* 위 -> 아래 수직 배치 */
      overflow: hidden;
    }

    /* body에는 배경 스타일만 주고, flex:1은 제거합니다 */
    body {
      min-height: 100vh; /* 최소 높이를 화면 전체 높이로 설정 */
      background-image: url('https://xjoospswraznhebpbavk.supabase.co/storage/v1/object/public/studiooven//compressed-background.webp');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
       
     
    }

    /* main이 남은 공간을 모두 차지하도록 */
    main {
      flex: 1; /* 푸터를 페이지 맨 밑으로 밀어내는 핵심 */
    }
  </style>
</head>

<body>
  <header>
    <!-- nav include -->
    <%- include('partials/mainNavbar') %>
    <h1 class="visually-hidden">스튜디오 오븐 — stuido.Oven</h1>
  </header>

  <main>
    <!-- 컨텐츠가 짧을 때는 화면의 빈 공간을 main이 차지,
         footer가 자동으로 화면 하단에 붙습니다.
         컨텐츠가 길면 스크롤이 생기고,
         footer가 컨텐츠 아래로 자연스레 밀립니다. -->
  </main>

  <footer>
    <%- include('partials/footer') %>
  </footer>

  <!-- Bootstrap JS (defer) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" defer></script>
</body>
</html>