<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>스튜디오 오븐</title>

  <!-- 메타, 부트스트랩, 스타일 불러오기 (질문 코드 그대로) -->
  <link rel="preload" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
        as="style" onload="this.rel='stylesheet'">
  <noscript>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  </noscript>
  <link rel="stylesheet" href="/style.css" />
  <style>
    /* 1) 화면 전체 높이 + flex 컨테이너로 만들기 */
    html, body {
      height: 100%;
      margin: 0;
      padding: 0;
      display: flex;           /* 플렉스 사용 */
      flex-direction: column;  /* 위->아래 */
    }

    main {
      flex: 1;  /* “나머지 공간”을 main이 차지 */
      /* background: #fafafa;  필요시 배경 등 */
    }

  </style>
</head>

<body>
  <header>
    <!-- nav include -->
    <%- include('partials/mainNavbar') %>
    <h1 class="visually-hidden">스튜디오 오븐 — stuido.Oven</h1>
  </header>

  <main>
    <!-- 컨텐츠가 짧을 때는 화면 빈 공간을 main이 차지,
         푸터가 자동으로 맨 아래 배치 됩니다.
         컨텐츠가 길면 스크롤 생기고, 푸터는 컨텐츠 아래로 밀려납니다. -->

  </main>

  <footer>
    <%- include('partials/footer') %>
  </footer>

  <!-- Bootstrap JS (defer) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" defer></script>
</body>
</html>