<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8" />
    <title>오버레이 투명 + 외부 클릭 닫힘</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />

    <style>
        body {
            background-image: url('https://linkup-mj12270411.s3.ap-northeast-2.amazonaws.com/background.png');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            min-height: 100vh;
            color: #042b6e;
            margin: 0;
        }

        /* ================================
           1) 오프캔버스 백드롭 투명 처리
        ================================ */
        .offcanvas-backdrop {
            background-color: transparent !important;
            /* 투명화 */
        }

        /* 혹은 필요하다면 opacity도 강제:
           .offcanvas-backdrop.show {
               opacity: 1 !important;
           }
        */

        .navbar-toggler-icon {
            display: none;
        }

        .navbar-brand,
        .navbar-toggler {
            align-self: center !important;
        }

        .navbar-brand img {
            display: block;
            width: 250px;
        }

        /* 햄버거 버튼 (2줄) */
        .navbar-toggler {
            position: relative;
            z-index: 1050;
            border: none;
            padding: 0.5rem;
            margin-right: 20px;

            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 16px;
            cursor: pointer;
        }

        .hamburger-bar {
            display: block;
            width: 30px;
            height: 5px;
            margin-bottom: 5px;
            background-color: #042b6e;
            border-radius: 3px;
            transition: transform 0.3s ease, opacity 0.3s ease;
            transform-origin: center;
        }

        /* 햄버거 → X */
        .navbar-toggler.open .top-bar {
            transform: translateY(5px) rotate(45deg);
        }

        .navbar-toggler.open .bottom-bar {
            transform: translateY(-5px) rotate(-45deg);
        }

        .mb-0 {
            color: #042b6e;
            font-size: 12px;
        }

        .menu-title {
            color: #042b6e;
            font-size: 20px;
        }

        .cheif {
            margin-bottom: 10px;
        }

        @media (min-width: 768px) {
            .navbar-brand {
                margin-left: 20px;
            }

            .cheif {
                margin-bottom: 20px;
            }
        }

        /* 오프캔버스 사이드바 (폭, 위치) */
        .offcanvas-custom-bg {
            background: url('https://linkup-mj12270411.s3.ap-northeast-2.amazonaws.com/sidebar.png') no-repeat center center / cover;
        }

        .offcanvas-body {
            overflow: visible !important;
            padding: 0;
        }

        #offcanvasRight.offcanvas-end {
            width: 20% !important;
            max-width: none !important;
            margin-right: 0 !important;
        }

        /* menu-block: 왼쪽 네모 + 줄 → 글자까지만 */
        .menu-block {
            position: relative;
            margin-bottom: 2rem;
            padding-bottom: 2rem;
            padding-left: 80px;
        }

        .menu-block::before {
            content: "";
            position: absolute;
            top: 5px;
            left: 0;
            width: 50px;
            height: 20px;
            background: #042b6e;
            z-index: 2;
        }

        .menu-block::after {
            content: "";
            position: absolute;
            display: inline-block;
            left: 0;
            right: 0;
            width: 60%;
            bottom: 0;
            height: 1px;
            background: #042b6e;
            z-index: 1;
        }

        .menu-title {
            position: relative;
            display: inline-block;
            font-weight: bold;
            font-size: 20px;
            cursor: pointer;
            margin-bottom: 20px;
            padding-bottom: 4px;
            transition: transform 0.3s ease;
            z-index: 3;
        }

        .menu-title:hover {
            transform: scale(1.05);
        }

        .menu-title::after {
            content: "";
            position: absolute;
            left: 100%;
            bottom: 0;
            width: 9999px;
            height: 2px;
            background: #fff;
            z-index: 3;
        }

        .sub-list {
            margin-left: 0;
        }

        .sub-item {
            display: inline-block;
            margin-bottom: 8px;
            color: #042b6e;
            text-decoration: none;
            position: relative;
            overflow: hidden;
            transition: color 0.3s ease;
        }

        .sub-item::after {
            content: "";
            position: absolute;
            left: 0;
            bottom: -1px;
            width: 100%;
            height: 2px;
            background-color: #042b6e;
            transform: scaleX(0);
            transform-origin: left;
            transition: transform 0.3s ease;
        }

        .sub-item:hover::after {
            transform: scaleX(1);
        }

        .collapse-list {
            display: none;
        }

        .collapse-list.show {
            display: block;
        }

        /* 푸터 (예시) */
        .footerMother {
            position: absolute;
            bottom: 0;
            left: 0;
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            margin-left: 10px;
            margin-bottom: 10px;
        }

        .footerMother img {
            width: 70px;
            margin-bottom: 8px;
        }

        .footerText {}

        .footer {
            margin: 0;
            font-size: 12px;
            color: #042b6e !important;
        }

        .verticalGroup {
            display: flex;
            flex-direction: column;
            gap: 8px;
            margin-bottom: 8px;
        }

        .horizontalGroup {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        @media (min-width: 768px) {
            .footerMother {
                flex-direction: row;
                align-items: flex-end;
                margin-left: 20px;
                margin-bottom: 20px;
            }

            .footerMother img {
                margin-bottom: 0;
                margin-right: 20px;
            }

            .footerText {
                display: flex;
                flex-direction: row;
                gap: 8px;
                align-items: flex-end;
            }

            .verticalGroup {
                flex-direction: column;
                margin-bottom: 0;
                gap: 8px;
            }

            .horizontalGroup {
                flex-direction: row;
                gap: 30px;
                align-items: center;
            }
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-light bg-transparent">
        <div class="container-fluid d-flex align-items-center justify-content-between" style="padding: 0;">
            <!-- 로고 -->
            <a class="navbar-brand fw-bold" href="/">
                <img src="https://linkup-mj12270411.s3.ap-northeast-2.amazonaws.com/logo.png" style="width: 200px;"
                    alt="로고" />
            </a>

            <!-- 햄버거 버튼 -->
            <button id="menuToggle" class="navbar-toggler d-block ms-auto" type="button">
                <span class="hamburger-bar top-bar"></span>
                <span class="hamburger-bar bottom-bar"></span>
            </button>

            <!-- data-bs-backdrop="true" + .offcanvas-backdrop → 투명 처리 -->
            <div class="offcanvas offcanvas-end offcanvas-custom-bg" data-bs-backdrop="true" tabindex="-1"
                id="offcanvasRight">
                <div class="offcanvas-header mt-5">
                    <h5 class="offcanvas-title"></h5>
                </div>
                <div class="offcanvas-body d-flex flex-column">
                    <!-- About us -->
                    <div class="menu-block">
                        <div class="menu-title">About us</div>
                        <div class="sub-list">
                            <a class="sub-item" href="#">studio.Oven</a><br />
                            <a class="sub-item" href="#">history</a><br />
                            <a class="sub-item" href="#">organization</a>
                        </div>
                    </div>

                    <!-- Projects -->
                    <div class="menu-block">
                        <div class="menu-title" data-target="#projectsSub">Projects</div>
                        <div id="projectsSub" class="sub-list collapse-list">
                            <% category.forEach(item=> { %>
                                <% if(item.id !==1) { %>
                                    <a class="sub-item" href="/project?id=<%= item.id %>">
                                        <%= item.categoryName %>
                                    </a><br />
                                    <% } %>
                                        <% }) %>
                        </div>
                    </div>

                    <!-- Contact -->
                    <div class="menu-block">
                        <div class="menu-title" data-target="#contactSub">Contact</div>
                        <div id="contactSub" class="sub-list collapse-list">
                            <a class="sub-item" href="#">Email</a><br />
                            <a class="sub-item" href="#">Telephone</a><br />
                            <a class="sub-item" href="#">Directions</a>
                        </div>
                    </div>

                    <!-- 하단 문구 -->
                    <div class="mt-auto text-center p-2 cheif">
                        <p class="mb-0">대표자 | 서정일 Jeong-il Seo / 이강민 Kang-min Lee</p>
                        <p class="mb-0 redirect-target">사업자등록번호 | 323 - 20 - 02200</p>
                    </div>
                </div>
            </div>
            <!-- offcanvas 끝 -->
        </div>
    </nav>

    <!-- 푸터 -->
    <div class="footerMother">
        <img src="https://linkup-mj12270411.s3.ap-northeast-2.amazonaws.com/vertical.png" alt="footerLogo" />
        <div class="footerText">
            <div class="verticalGroup">
                <p class="footer">
                    스튜디오 오븐 studio.Oven [Interior&Design]
                </p>
                <p class="footer">
                    주소 ㅣ 서울 강남구 논현로 132길 11, 302호
                </p>
            </div>
            <div class="horizontalGroup">
                <p class="footer">TEL ㅣ 02-6448-0308</p>
                <p class="footer">FAX ㅣ 02-6448-0309</p>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js">
    </script>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const menuToggle = document.getElementById("menuToggle");
            const offcanvasElement = document.getElementById("offcanvasRight");
            const offcanvasMenu = new bootstrap.Offcanvas(offcanvasElement);

            // 클릭 → 햄버거(X) 전환 + 오프캔버스 열고 닫기
            menuToggle.addEventListener("click", function () {
                if (offcanvasElement.classList.contains("show")) {
                    offcanvasMenu.hide();
                    this.classList.remove("open");
                } else {
                    offcanvasMenu.show();
                    this.classList.add("open");
                }
            });

            // Projects/Contact → 제목 클릭 시 서브 항목 열기/닫기
            document.querySelectorAll(".menu-title[data-target]").forEach(title => {
                title.addEventListener("click", function () {
                    const targetId = this.getAttribute("data-target");
                    const targetEl = document.querySelector(targetId);
                    if (!targetEl) return;
                    targetEl.classList.toggle("show");
                });
            });

            // 숨은 관리자 페이지 이동 (5번 클릭)
            let clickCount = 0;
            let timer;
            const redirectEl = document.querySelector(".redirect-target");
            if (redirectEl) {
                redirectEl.addEventListener("click", function () {
                    clickCount++;
                    clearTimeout(timer);
                    timer = setTimeout(() => {
                        clickCount = 0;
                    }, 800);
                    if (clickCount >= 5) {
                        window.location.href = "/manage";
                    }
                });
            }
        });
    </script>
</body>

</html>

