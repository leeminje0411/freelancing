<nav class="navbar navbar-expand-lg navbar-light bg-light px-3">
    <!-- 왼쪽 대시보드 문구 -->
    <a class="navbar-brand fw-bold" href="#">대시보드</a>

    <!-- 모바일에서 햄버거 메뉴 -->
    <button class="navbar-toggler" type="button" id="menuToggle">
        <span class="navbar-toggler-icon"></span>
    </button>

    <!-- ✅ PC에서는 메뉴가 우측 정렬 -->
    <div class="collapse navbar-collapse justify-content-end" id="navMenu">
        <ul class="navbar-nav">
            <li class="nav-item"><a class="nav-link" href="/upload">업로드</a></li>
            <li class="nav-item"><a class="nav-link" href="/edit">게시글 수정/삭제</a></li>
            <li class="nav-item"><a class="nav-link" href="/visitors">방문자</a></li>
        </ul>
    </div>
</nav>

<!-- ✅ 모바일에서 오른쪽에서 슬라이드하는 햄버거 메뉴 -->
<div id="sideMenu" class="side-menu">
    <button id="closeMenu" class="close-btn">&times;</button>
    <ul>
        <li><a href="#">업로드</a></li>
        <li><a href="#">게시글 수정/삭제</a></li>
        <li><a href="#">방문자</a></li>
    </ul>
</div>

    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const menuToggle = document.getElementById("menuToggle");
            const sideMenu = document.getElementById("sideMenu");
            const closeMenu = document.getElementById("closeMenu");

            // ✅ 햄버거 메뉴 클릭 시 오른쪽에서 메뉴 등장
            menuToggle.addEventListener("click", function () {
                sideMenu.classList.add("show");
            });

            // ✅ 닫기 버튼 클릭 시 메뉴 숨기기
            closeMenu.addEventListener("click", function () {
                sideMenu.classList.remove("show");
            });

            // ✅ 화면 클릭 시 메뉴 닫기 (메뉴 외부 클릭 감지)
            document.addEventListener("click", function (event) {
                if (!sideMenu.contains(event.target) && !menuToggle.contains(event.target)) {
                    sideMenu.classList.remove("show");
                }
            });
        });
    </script>